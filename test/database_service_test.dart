import 'package:flutter_test/flutter_test.dart';

void main() {
  test("description", () {
    /// Setup of test case :
    final sum = 1 + 0;

    /// expect
    expect(sum, 1);
  });
  test("getAllPost", () {
    /// Setup of test case :
    final sum = 1 + 0;

    /// expect
    expect(sum, 1);
  });
}
